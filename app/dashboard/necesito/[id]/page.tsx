import Link from "next/link"
import { ArrowLeft, Clock, MapPin, MessageSquare, ShieldCheck } from "lucide-react"
import { Button } from "@/components/ui/button"

export default async function NeedDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  return <div className="space-y-5"><Link href="/dashboard/necesito" className="inline-flex items-center gap-1 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft className="h-4 w-4" /> Necesidades</Link><article className="overflow-hidden rounded-[2rem] border border-amber-200 bg-white shadow-lg"><header className="bg-gradient-to-br from-amber-500 to-orange-600 p-6 text-white"><div className="flex justify-between"><span className="rounded-full bg-white/20 px-3 py-1 text-xs font-bold">Educación</span><span className="text-xs text-white/80">#{id}</span></div><h1 className="mt-5 text-2xl font-bold">Necesito profesora particular de matemáticas para secundaria</h1><div className="mt-3 flex flex-wrap gap-3 text-xs text-white/85"><span className="flex items-center gap-1"><MapPin className="h-4 w-4" /> Hudson</span><span className="flex items-center gap-1"><Clock className="h-4 w-4" /> Esta semana</span></div></header><div className="space-y-5 p-6"><div><h2 className="font-bold">Detalle</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Necesito apoyo en álgebra y geometría, preferentemente a domicilio dos veces por semana. Los datos de contacto se comparten al responder.</p></div><div className="rounded-2xl bg-amber-50 p-4"><div className="flex items-center gap-2 font-bold text-amber-900"><MessageSquare className="h-4 w-4" /> 3 respuestas recibidas</div><p className="mt-1 text-xs text-amber-800">Respondé solo si podés resolver esta necesidad en la zona indicada.</p></div><Button className="w-full bg-amber-600 text-white hover:bg-amber-700">Ofrecer una solución</Button><p className="flex items-center justify-center gap-1 text-xs text-muted-foreground"><ShieldCheck className="h-4 w-4" /> VEZI no procesa pagos. Coordiná alcance y condiciones directamente.</p></div></article></div>
}
